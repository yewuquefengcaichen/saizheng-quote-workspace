# 项目阶段进度（赛正报价工作台）

## 1. 当前项目定位（一句话）

这是一个**可直接用于业务报价**的工作台，同时在持续完成从 legacy 数据形态到 PostgreSQL / V2 架构的升级。

---

## 2. 当前做到哪一阶段

当前处于：**v0.9.1 上线前最后收口**。

这轮已经跨过 6 个关键节点：

- 历史页 API 已以 PostgreSQL V2 为主读源
- 词库已切到 PostgreSQL 主读写，JSON 保留为双写回退
- 模板模块已切到 PostgreSQL 主读写，JSON 保留为双写回退
- 图搜图 Redis 查询缓存第一版已落地
- 图搜图历史反馈精排第一版已落地
- CLIP 全量向量已完成，当前 `clip_local / openclip_vit_b_32_512d` 为 **3300 / 3300**，`pending_assets = 0`

---

## 3. 四个里程碑与状态

| 里程碑 | 目标 | 当前状态 |
| --- | --- | --- |
| 里程碑 1：稳定工作台 | 报价流程可用、可回退、可日常使用 | 已完成 |
| 里程碑 2：数据收口 | 历史 / 词库 / 模板 / 商品链路逐步以 PostgreSQL 为主 | 收口中 |
| 里程碑 3：图搜图生产化 | CLIP 全量向量、缓存、反馈精排、任务化 | 收口中 |
| 里程碑 4：新架构重构 | 前后端契约收敛、组件化重构、性能基线 | 未开始 |

---

## 4. 当前最重要的已完成项

- 首页上传报价后，已直接进入 `/quotes`，不再被首页大弹层打断。
- 列映射确认改为报价台内嵌，确认路径更短。
- 报价台已形成“左侧处理原始项 + 右侧选品”的高密度工作流。
- 商品库、历史页、模板、图搜图链路已接入 PostgreSQL V2 主链能力。
- 词库与模板都已完成 PostgreSQL 主读写桥接，保留 JSON 双写回退。
- 图搜图 Redis 查询缓存第一版已完成，Redis 不可用时会自动降级。
- 图搜图历史反馈精排第一版已完成，命中历史确认结果时会做保守加分。
- CLIP 批处理脚本已完成首轮全量生成，当前 `clip_local / openclip_vit_b_32_512d` ready 数量为 **3300**，`pending_assets = 0`。
- 商城抓取、同步任务、图搜图任务已具备后台任务化入口。
- 当前版本具备稳定回退点（可作为阶段性可用版本保留）。

---

## 5. 还差什么才算阶段性收口

要把“阶段 2 + 阶段 3”算作一次完整收口，当前还差这几项：

- 做一次核心回归：报价台 / 历史 / 词库 / 模板 / 图搜图。
- 图搜图缓存 + 反馈精排继续做上线前效果观察，必要问题只记录，不在本轮扩需求。
- 文档、版本标记、回退说明、Release 草稿与当前实际行为完全一致，并完成版本冻结 / push / 上线准备。

---

## 6. 下一次停下来的阶段点（避免无限开发）

**下一次明确停下来的阶段点**：

完成下面 4 条后，先停一次并冻结 `v0.9.1`：

1. 核心回归通过：报价台 / 历史 / 词库 / 模板 / 图搜图。
2. README + CHANGELOG + 进度文档 + Release 草稿同步完成。
3. commit / tag / push / 回退锚点确认完成。
4. 上线准备项确认完成（运行方式、回退方式、数据位置、风险备注）。

到这个点就视为“当前阶段完成”，先冻结一版，再进入下一轮。

---

## 7. 当前进度判断结论

- 现在不是“无限写代码”的状态。
- 当前目标应聚焦为：**v0.9.1 上线前最后收口并冻结版本**。
- 数据收口这条线已经拿下“历史页 + 词库 + 模板”三个关键模块。
- 接下来最值钱的收口动作是：**核心回归 + 文档齐套 + 版本冻结 + 上线准备**。
