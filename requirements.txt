# 一键报价系统依赖

# Web框架
flask>=2.3.0

# 数据处理
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0
xlrd>=2.0.0

# 中文分词
jieba>=0.42.0

# AI服务依赖（可选）
anthropic>=0.18.0      # Claude API
openai>=1.0.0          # OpenAI API
sentence-transformers  # 本地模型（降级方案）

# 异步支持
aiohttp>=3.9.0
sqlalchemy>=2.0.40
asyncpg>=0.30.0
pgvector>=0.4.0

# 配置文件
pyyaml>=6.0
python-dotenv>=1.0.1
pydantic-settings>=2.10.0

# PDF导出（可选）
reportlab>=4.0.0       # PDF生成
weasyprint>=60.0       # HTML转PDF（更美观）

# OCR（可选）
# easyocr>=1.7.0

# 商城抓取同步（可选，首次使用需执行 playwright install chromium）
playwright>=1.50.0

# 图片归档 / 图搜图
Pillow>=11.0.0
ImageHash>=4.3.0

# 图搜图第二阶段：CLIP 512 维视觉 embedding（RTX 4060 验证通过的 CUDA 版）
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.5.1+cu121
torchvision==0.20.1+cu121
torchaudio==2.5.1+cu121
open_clip_torch>=3.3.0
