# 一键报价系统 v2.0 - 项目记忆

## 0. 工程硬规则（2026-04-16 新增）

### 0.1 Windows 终端出现问号 / 乱码的根因

这次已经**实测确认**，根因不是项目文件坏了，而是 **Windows PowerShell 5.1 默认编码链有坑**：

1. **`Get-Content` 默认不会可靠按 UTF-8 无 BOM 读取**
   - 本项目现在统一是 **UTF-8 + LF**
   - 但 Windows PowerShell 5.1 里直接：
     - `Get-Content README.md`
   - 很容易按本地 ANSI / CP936 去解码
   - 结果就会把正常中文显示成：
     - `璧涙鎶ヤ环宸ヤ綔鍙?`
   - 这叫 **mojibake（错码乱码）**

2. **`$OutputEncoding` 在 PowerShell 5.1 默认是 `us-ascii`**
   - 这会导致把带中文的 here-string / 管道文本喂给 `python -`、`node -`、其他 stdin 命令时
   - 中文在进入子进程前就先被替换成 `?`
   - 所以会看到：
     - `???????????? / ??? / ?????`
   - 这不是 Python 逻辑错，是 **PowerShell 管道输出时先把中文毁了**

3. **WSL2 / Ubuntu 默认 UTF-8 环境通常不会出现这个问题**
   - 当前已实测：
     - `LANG=C.UTF-8`
     - Python stdout / filesystem / preferred encoding 都是 `utf-8`
   - 同样的中文输出在 WSL2 下是正常的
   - 但如果 WSL 继承了 Windows 侧带中文且已损坏的 PATH 片段，仍可能在启动日志里看到：
     - `sw���`
   - 那是 **Windows 环境变量路径翻译噪音**
   - **不是项目文件内容损坏**

### 0.2 已确认的实测结论

- `Get-Content README.md`：乱码
- `Get-Content README.md -Encoding UTF8`：正常
- `python -X utf8 -` 且脚本经 PowerShell 默认管道输入：中文会变 `?`
- 先把 `$OutputEncoding` 改成 UTF-8，再把脚本 pipe 给 Python：中文恢复正常
- WSL2 `bash + python3`：中文正常

### 0.3 以后禁止再犯的规则

#### 规则 A：在 Windows PowerShell 5.1 里读 UTF-8 文件

**禁止直接默认：**

```powershell
Get-Content README.md
```

**必须改成：**

```powershell
Get-Content README.md -Encoding UTF8
```

或者直接用：

```powershell
@'
from pathlib import Path
print(Path('README.md').read_text(encoding='utf-8'))
'@ | python -X utf8 -
```

#### 规则 B：在 Windows PowerShell 5.1 里把带中文脚本 pipe 给 Python / Node

默认前必须先设 UTF-8：

```powershell
$utf8 = New-Object System.Text.UTF8Encoding($false)
$OutputEncoding = $utf8
[Console]::InputEncoding = $utf8
[Console]::OutputEncoding = $utf8
chcp 65001 > $null
```

然后再执行：

```powershell
@'
print('中文测试')
'@ | python -X utf8 -
```

#### 规则 C：PowerShell 里尽量避免把“中文绝对路径”直接写进 pipe 脚本

优先用：

- 相对路径
- `Path('docs/xxx.md')`
- 已进入项目目录后的相对访问

不要在默认 PowerShell 管道链路里硬塞：

- `D:\中文目录\中文目录\xxx`

因为中文字面量本身就可能先被 PowerShell 的 ASCII 管道破坏。

#### 规则 D：能用 WSL2 / bash 做文本检查时，优先用 WSL2 / bash

尤其适合：

- `cat`
- `python3`
- `rg`
- `sed`
- `awk`
- UTF-8 文本核对

#### 规则 E：禁止在 Windows PowerShell 5.1 用默认编码写仓库文本文件

以下操作默认都视为**高风险**：

- `Set-Content`
- `Out-File`
- `>`
- `>>`

如果必须写文件：

1. **优先用 `apply_patch`**
2. 或显式指定 UTF-8
3. 或直接用 Python / `Path.write_text(..., encoding='utf-8')`

最低要求：

```powershell
$utf8 = New-Object System.Text.UTF8Encoding($false)
[System.IO.File]::WriteAllText('README.md', $content, $utf8)
```

禁止再出现：

- “终端里看着正常，文件实际被写坏”
- “PowerShell 默认编码把中文落盘成问号”

#### 规则 F：前端出现问号 / 乱码时，先按固定顺序排查

以后不允许一看到浏览器问号，就直接判断“仓库文件坏了”。

统一按这个顺序排查：

1. **先看源码**
   - `Get-Content -Encoding UTF8`
   - 或 Python `read_text(encoding='utf-8')`
2. **再看浏览器网络响应**
   - HTML / JS / CSS response 里中文是否正常
3. **再看静态资源版本号**
   - `?v=...` 是否还是旧值
4. **强刷 / 清缓存 / 重启服务**
   - `Ctrl + F5`
   - 重启 Flask / waitress
5. 如果模板没问题、接口字段乱码：
   - **优先查 API 返回**
   - **再查数据库 / JSON / SQLite 真实数据**

判断原则：

> **终端乱码 ≠ 文件损坏**  
> **浏览器问号 ≠ 仓库全坏**  
> 必须先用 UTF-8 显式复核，再下结论。

### 0.4 本项目以后统一执行标准

1. **仓库文本文件统一 UTF-8**
2. **默认 LF**
3. 已加入：
   - `.editorconfig`
   - `.gitattributes`
4. 在 Windows PowerShell 5.1 中：
   - 读取 UTF-8 文件必须显式声明编码
   - 管道喂脚本前必须先切 UTF-8
   - 默认编码写文本文件视为违规操作
5. 以后看到问号 / 乱码，先排查：
   - 是文件坏了？
   - 还是终端解码错了？
   - 还是 PowerShell 管道把中文替换成 `?` 了？
   - 还是浏览器缓存 / 旧进程 / 旧静态资源版本？
6. 只要任务允许，**UTF-8 重文本检查优先用 WSL2 / bash**

> 结论：**这不是“项目中文不能用”，而是 Windows PowerShell 5.1 默认编码行为不可靠。以后按本节规则执行，不允许再犯。**

## 版本说明

| 版本 | 路径 | 状态 | 说明 |
|------|------|------|------|
| **v1.0** | `quote_system/` | ✅ 可用版本 | 保留备份，可随时回退 |
| **v2.0** | `claudecode-报价系统/` | 🔧 开发版本 | 在此进行新功能开发 |

---

## 项目概述

**项目名称**: 赛正慧采商城一键报价系统
**创建时间**: 2026-03-16
**当前版本**: v2.0-dev

**核心目标**: 将人工手动搜索报价的流程自动化

```
原流程: 报价单 → 人工搜索商城 → 逐个比对 → 记录价格 → 标记无货
新流程: 报价单上传 → 智能解析 → 自动匹配 → 人工确认 → 导出报价单
```

---

## 数据存储说明

### 当前方式
- **商品库**: JSON文件 (`data/products.json`)
- **同义词库**: JSON文件 (`data/synonyms.json`)
- **特点**: 每次上传Excel覆盖旧数据

### 为什么用JSON不用SQLite？
1. 5841条数据，JSON读写够快
2. 简单直接，无需数据库配置
3. 新增商品只需重新上传Excel即可

### 数据更新流程
```
商城导出新Excel → 上传到系统 → 覆盖products.json → 完成
```

---

## 技术架构

```
claudecode-报价系统/
├── app.py                 # Flask后端主程序
├── 启动系统.bat           # 双击启动脚本
├── PROJECT_MEMORY.md      # 本文件（项目记忆）
├── templates/
│   └── index.html         # 网页界面
├── utils/
│   ├── matcher.py         # 智能匹配引擎
│   └── excel_handler.py   # Excel处理
├── data/
│   ├── products.json      # 商品库（上传后生成）
│   └── synonyms.json      # 同义词库
└── output/                # 导出的报价单
```

---

## 数据信息

### 商城商品库
- **文件**: `完整3.16赛正慧采商城商品导出信息.xls`
- **商品数**: 5841个
- **主要品类**: 劳保用品(手套、安全鞋、口罩、安全帽等)、五金工具
- **关键字段**: 商品编码、商品名称、型号、商品类别、单位、市场价、成本价、品牌、供应商

### 报价单示例
- **文件**: `3.14需要从赛正慧采商城报价的报价单.xls`
- **报价项**: 34项（实际解析32项）
- **特点**: 多行表头，备注含GB标准等规格要求

---

## v1.0 已完成功能

### 基础功能
- [x] Flask后端框架
- [x] 网页界面（Bootstrap 5）
- [x] 商品库上传导入
- [x] 报价单上传解析（特定格式）
- [x] 导出报价单Excel

### 匹配功能
- [x] 智能匹配引擎（精准+模糊+同义词）
- [x] 自动匹配高匹配度商品（≥50%）
- [x] 同义词管理界面

### 界面功能
- [x] 匹配结果展示
- [x] 展开全部候选商品
- [x] 成本价显示
- [x] 搜索和筛选功能
- [x] 人工选择确认/修改

---

## v2.0 开发进度

### 阶段一：智能解析（最高优先级）✅ 已完成

#### 任务 #7: 智能列识别 ✅ 已完成
**状态**: 已完成 (2026-03-16)

**实现功能**:
- `utils/smart_parser.py` - SmartQuoteParser类
- 自动检测多行表头（最多5行）
- 关键词匹配打分识别列类型
- 数据特征分析辅助识别
- 横向布局检测与自动转置
- 大数字(>100)检测区分数据行

**识别规则**:
| 列类型 | 关键词 |
|--------|--------|
| 名称列 | 产品名称/物资名称/品名/商品名/名称/Item/Product/货品 |
| 数量列 | 数量/需求数量/需求量/Qty/Quantity/Count |
| 单位列 | 单位/计量单位/UOM/Unit/单位名称 |
| 价格列 | 单价/预算单价/价格/报价/Price/单价（元）/含税单价 |
| 规格列 | 规格/型号/规格型号/Spec/型号规格 |
| 备注列 | 备注/说明/要求/技术参数/Remark/备注说明 |
| 序号列 | 序号/NO/编号/No./序 号 |
| 品牌列 | 品牌/brand/生产厂家/厂商 |
| 分类列 | 类别/分类/category/类型 |

#### 任务 #8: 列映射确认界面 ✅ 已完成
**状态**: 已完成 (2026-03-16)

**实现功能**:
- 前端模态框显示列映射确认界面
- 置信度显示（绿色≥80%，黄色≥50%，灰色<50%，红色未识别）
- 下拉框可修改列类型
- 数据预览（前5行）
- 必须选择"产品名称"列才能继续

**API端点**:
- POST `/api/smart_parse` - 智能解析报价单
- POST `/api/confirm_mapping` - 确认列映射并提取报价项

#### 任务 #9: 解析模板保存 ✅ 已完成
**状态**: 已完成 (2026-03-17)

**实现功能**:
- 保存列映射配置为模板
- 自动匹配相似格式的文件
- 模板标识符提取和匹配算法
- 模板管理（保存、删除）

**API端点**:
- GET `/api/get_templates` - 获取所有模板
- POST `/api/save_template` - 保存模板
- POST `/api/delete_template` - 删除模板

**数据存储**: `data/parse_templates.json`

---

### 阶段二：匹配增强

#### 任务 #6: 替代品推荐 ✅ 已完成
**状态**: 已完成 (2026-03-17)

**实现功能**:
- 后端 `utils/matcher.py` 新增 `find_alternatives()` 方法
- 基于商品类别推荐同类商品
- 基于关键词相似度推荐
- 显示推荐原因（同类商品/关键词相似）
- 前端显示替代品卡片，支持点击选择

**触发条件**:
- 无匹配商品时
- 最佳匹配度 < 40% 时

---

### 阶段三：业务功能

#### 任务 #14: 利润计算显示 ✅ 已完成
**状态**: 已完成 (2026-03-17)

**实现功能**:
- 显示成本价vs预算单价
- 自动计算利润和利润率
- 无成本价时显示市场价差异
- 绿色/红色区分盈利/亏损

**显示逻辑**:
```
有成本价: 利润 = 预算价 - 成本价, 利润率 = 利润 / 成本价 × 100%
无成本价: 显示与市场价差异（高于/低于市场价）
```

#### 任务 #11: 价格对比 ✅ 已完成
**状态**: 已完成 (2026-03-17)

**实现功能**:
- 后端 `utils/matcher.py` 新增 `find_supplier_variants()` 方法
- 根据产品名称查找不同供应商的价格
- 前端显示价格对比模态框
- 显示市场价、成本价、利润对比
- 最低成本价标记
- 支持选择不同供应商的产品

**界面示例**:
```
布手套 - 3个供应商
├─ 供应商A: 市场价¥1.80 成本价¥0.90 [选择] ✓ 最低
├─ 供应商B: 市场价¥1.65 成本价¥0.85 [选择]
└─ 供应商C: 市场价¥1.90 成本价¥1.00 [选择]
```

#### 任务 #12: 批量价格调整 ✅ 已完成
**状态**: 已完成 (2026-03-17)

**实现功能**:
- 批量调价模态框界面
- 四种调整方式：加价比例、折扣比例、固定加价、固定减价
- 应用范围选择：全部商品或仅已匹配商品
- 实时预览调整效果
- 调整后价格显示（原价划线，新价格高亮）
- 导出时自动应用价格调整
- 清除调整功能

**调整类型**:
| 类型 | 说明 |
|------|------|
| markup | 按比例加价 (原价 × (1 + X%)) |
| markdown | 按比例折扣 (原价 × (1 - X%)) |
| fixed_markup | 固定金额加价 (原价 + X元) |
| fixed_markdown | 固定金额减价 (原价 - X元) |

**前端组件**:
- `#batchAdjustBtn` - 批量调价按钮
- `#batchAdjustModal` - 调价模态框
- `#priceAdjustNotice` - 调整提示条
- `getAdjustedPrice()` - 计算调整后价格

**后端修改**:
- `utils/excel_handler.py` - export_quote()方法支持price_adjustment参数
- `app.py` - 导出API接收并传递价格调整配置

---

### 阶段四：导出增强

#### 任务 #13: 导出格式选择 ✅ 已完成
**状态**: 已完成 (2026-03-17)

**实现功能**:
- 导出选项模态框
- 三种导出格式选择
- 价格类型选择（市场价/调整后价格/成本价）
- 客户名称输入
- 格式兼容性检查

**导出格式**:
| 格式 | 说明 | 包含字段 |
|------|------|----------|
| standard | 标准报价单 | 序号、产品名称、数量、单价、总价、品牌 |
| internal | 内部报价单 | 市场价、成本价、报价单价、利润、利润率、供应商 |
| statement | 对账单格式 | 商品名称、数量、单价、金额、供应商 |

**价格选项**:
- `market`: 使用市场价
- `adjusted`: 使用调整后价格
- `cost`: 使用成本价（仅内部格式可用）

**前端组件**:
- `#exportOptionsModal` - 导出选项模态框
- `showExportOptions()` - 显示导出选项
- `performExport()` - 执行导出

**后端修改**:
- `utils/excel_handler.py` - export_quote()支持format、price_type参数
- `app.py` - 导出API接收并传递导出配置

---

### 阶段五：数据管理

#### 任务 #10: 报价历史记录 ✅ 已完成
**状态**: 已完成 (2026-03-17)

**实现功能**:
- SQLite数据库存储 (`utils/database.py`)
- 报价记录表和明细表
- 统计数据：总报价次数、本月报价、总金额、平均匹配率
- 历史记录列表展示
- 报价详情查看
- 导出时自动保存历史记录

**数据库结构**:
```
quote_history: id, created_at, customer_name, quote_file, total_items, matched_items, total_amount, export_file
quote_items: id, quote_id, item_name, item_quantity, product_name, supplier, match_score
```

**API端点**:
- GET `/api/get_quote_history` - 获取历史记录列表
- GET `/api/get_quote_detail/<id>` - 获取报价详情
- POST `/api/save_quote_history` - 保存报价记录
- DELETE `/api/delete_quote_history/<id>` - 删除记录
- GET `/api/get_quote_statistics` - 获取统计数据

---

## 用户偏好记录

1. **价格使用市场价**（售价），不是成本价
2. **匹配阈值**: ≥50%自动选择
3. **同义词可手动添加**
4. **输出格式**: 按商城商品库格式，后续可改

---

## 已解决问题记录

### 问题1: NaN值导致JSON解析失败
- **原因**: Excel空值被pandas读取为NaN
- **解决**: 添加`safe_value()`和`clean_nan()`函数处理

### 问题2: 多行表头解析失败
- **原因**: 报价单表头占多行，格式不统一
- **解决**: 重写解析逻辑，遍历识别数据行

### 问题3: 代理影响CDN加载
- **原因**: 用户使用Hiddify代理
- **解决**: 更换CDN源为unpkg

---

## 启动方式

```bash
# 方式1: 双击启动
双击 启动系统.bat

# 方式2: 命令行启动
cd D:\A赛正\完整导出的商品信息\claudecode-报价系统
python app.py

# 访问地址
http://localhost:5000
```

---

## 开发规范

### 版本管理
- v1.0 (`quote_system/`) - 保持不变，作为稳定版本
- v2.0 (`claudecode-报价系统/`) - 开发新功能

### 开发流程
1. 每次开发新功能前，更新本文件的任务状态
2. 完成一个任务后，标记完成并测试
3. 如果开发失败，可回退到v1.0重新开始

### 代码规范
- 后端: Python Flask
- 前端: HTML + Bootstrap 5 + Vanilla JS
- 数据: JSON文件存储
- 编码: UTF-8

---

## 更新日志

### 2026-03-16 v1.0
- 创建项目基础功能
- 完成商品库上传、报价单解析
- 完成智能匹配引擎
- 完成同义词管理
- 完成导出报价单

### 2026-03-16 v2.0-dev
- 创建开发版本副本
- 创建项目记忆文件
- 规划新功能开发任务清单
- ✅ 任务 #7: 完成智能列识别功能
- ✅ 任务 #8: 完成列映射确认界面
- 新增 `utils/smart_parser.py` 智能解析器
- 新增 `/api/smart_parse` 和 `/api/confirm_mapping` API端点
- 新增列映射确认模态框UI
- 测试通过：34个报价项成功解析和匹配

### 2026-03-17 v2.0-dev
- ✅ 任务 #9: 完成解析模板保存功能
- 新增模板匹配算法（基于标识符交集）
- 新增模板管理API端点
- 新增前端"保存为模板"功能
- 模板自动匹配：上传相似格式文件时自动应用已保存的映射
- ✅ 任务 #14: 完成利润计算显示功能
- 商品卡片显示成本价和利润计算
- 无成本价时显示市场价差异
- 自动计算利润率和价格差异百分比
- ✅ 任务 #6: 完成替代品推荐功能
- 无匹配或低匹配度时显示推荐替代品
- 基于商品类别和关键词相似度推荐
- 前端替代品卡片支持点击选择
- ✅ 任务 #11: 完成价格对比功能
- 点击商品卡片的价格对比按钮查看不同供应商价格
- 显示市场价、成本价、利润对比
- ✅ 任务 #10: 完成报价历史记录功能
- SQLite数据库存储报价历史
- 历史记录列表和详情查看
- 统计数据展示
- ✅ 任务 #12: 完成批量价格调整功能
- 支持四种调价方式：比例加价/折扣、固定加价/减价
- 商品卡片实时显示调整后价格
- 导出时自动应用价格调整
- ✅ 任务 #13: 完成导出格式选择功能
- 三种导出格式：标准报价单、内部报价单、对账单
- 支持市场价、调整后价格、成本价选择
- 客户名称输入和文件命名

### 品牌匹配增强 (2026-03-17)
- 匹配引擎增加品牌匹配加分逻辑
- 支持从报价单识别品牌列
- 支持从产品名称自动提取品牌
- 品牌完全匹配给予高分(1.0)，名称包含品牌给予中分(0.8)
- 品牌匹配权重占比20%
- 支持品牌别名映射（如SZ→赛正、XY→星宇等）
- 最低成本价标记
